{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "uv: sync",
      "type": "shell",
      "command": "uv",
      "args": [
        "sync"
      ],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "CLAWAKE_WORKSPACE_ROOT": "${workspaceFolder}"
        }
      },
      "problemMatcher": []
    }
  ]
}
