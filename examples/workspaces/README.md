# workspaces

Enthaelt Beispiel-Workspace-Strukturen mit klar getrennten Mount-Zielen:

- `workspace/` fuer den Arbeitskontext im Container (`/workspace`)
- `role/` fuer Team-Definitionen (`/team-definition`)
