# Examples

This directory contains example staff data and role-specific workspaces. OpenClaw uses the workspace directories as active working directories, while runtime config/state data is stored in host runtime paths outside these workspace folders by default.

Committing workspace content can be useful for reproducible templates and onboarding examples, but be careful not to commit secrets, local tokens, or other sensitive runtime artifacts.
